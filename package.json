{
  "name": "meta-scraper",
  "main": "index.js",
  "scripts": {
    "dev": "micro-dev",
    "start": "micro -p $PORT",
    "format": "prettier --write \"index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "html-metadata": "^1.7.0",
    "micro": "^9.1.0"
  },
  "devDependencies": {
    "micro-dev": "^2.2.0",
    "prettier": "^1.11.1"
  }
}
