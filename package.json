{
  "name": "meta-scraper",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "micro": "^9.1.0"
  },
  "devDependencies": {
    "micro-dev": "^2.2.0",
    "prettier": "^1.10.2"
  }
}
